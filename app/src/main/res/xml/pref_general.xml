<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory android:title="Main Settings">
       <!-- <CheckBoxPreference
            android:key="Main_Option"
            android:title="Main Option"
            android:defaultValue="true"
            android:summary="SUMMARY_Main_Option" />-->
        <ListPreference
            android:title="Change default tip %"
            android:summary="%s"
            android:key="listPref"
            android:defaultValue="20"
            android:entries="@array/percent_list"
            android:entryValues="@array/percent_values" />


    </PreferenceCategory>

   <!-- <CheckBoxPreference
        android:key="percent_list"
        android:title="Default Tip"
        android:summary="Set the default tip to value in this list"
        android:defaultValue="20"
        android:entries=""/>-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--<EditTextPreference
        android:key="example_text"
        android:title="@string/pref_title_display_name"
        android:defaultValue="@string/pref_default_display_name"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference
        android:key="percent_list"
        android:title="Default Tip"
        android:summary="Set the default tip to value in this list"
        android:defaultValue="20"
        android:entries="@array/percent_list"
        android:entryValues="@array/percent_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />-->

</PreferenceScreen>
